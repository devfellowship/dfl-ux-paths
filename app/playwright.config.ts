import { defineConfig, devices } from "@playwright/test";

// e2e runs fully offline against the built preview server: all specs use
// `repo=demo/...` fixture targets (src/lib/api.ts), which are read straight
// from public/fixtures/*.json — no GitHub calls, no GITHUB_READ_TOKEN needed.
export default defineConfig({
  testDir: "./e2e",
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 1 : 0,
  reporter: process.env.CI ? "github" : "list",
  use: {
    baseURL: "http://localhost:4173",
    trace: "on-first-retry",
  },
  webServer: {
    command: "npm run build && npm run preview",
    url: "http://localhost:4173",
    reuseExistingServer: !process.env.CI,
    timeout: 120_000,
  },
  projects: [{ name: "chromium", use: { ...devices["Desktop Chrome"] } }],
});
